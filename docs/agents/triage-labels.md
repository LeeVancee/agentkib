# Triage Labels

This repo does not use an issue tracker, so triage labels are not applicable.

Do not create or apply the canonical labels:

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

Incoming work should be clarified and tracked in the conversation or in a repository document specified by the user.
